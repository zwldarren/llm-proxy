"""Tracing provider implementations.

Each subpackage here implements one tracing backend. Shared handler
machinery (base class, registry, built-in handlers) stays in the parent
`handlers` package.
"""
