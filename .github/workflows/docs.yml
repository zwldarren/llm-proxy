name: Docs

on:
  push:
    branches: ["main"]
    paths: &docs-paths
      - "docs/**"
      - ".github/workflows/docs.yml"
  pull_request:
    branches: ["main"]
    paths: *docs-paths
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    # Builds run PR-supplied code, so they get read-only Pages access and
    # never an OIDC token; the deploy job below holds the write permissions.
    permissions:
      contents: read
      pages: read
    steps:
      - uses: actions/checkout@v7
      - name: Set up bun
        uses: oven-sh/setup-bun@v2
      - name: Setup Pages
        uses: actions/configure-pages@v6
      - name: Install docs dependencies
        run: bun install --frozen-lockfile
        working-directory: docs
      - name: Build site
        run: bun run docs:build
        working-directory: docs
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: docs/.vitepress/dist

  deploy:
    needs: build
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
